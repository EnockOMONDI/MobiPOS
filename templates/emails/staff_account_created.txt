Hi {{ user.first_name|default:user.email }},

Your MobiPOS account for {{ organization.name }} is ready.

Added by:
{{ created_by.get_full_name|default:created_by.username }}

Sign-in email:
{{ user.email }}

Create your private password using this secure, single-use link. It expires on {{ expires_at|date:"d M Y, H:i" }}.

Set up your account:
{{ setup_url }}
